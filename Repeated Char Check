

// JAVA 8


import java.util.*;
import java.util.function.Function;
import java.util.stream.*;

public class Main {

	public static void main(String[] args) {

		Main obj = new Main();

		String s = "Hello world";

		s = s.replaceAll("\\s+", "");

		// All Reapting Characters
		Map<String, Long> hashmap = s.chars().mapToObj(c -> (char) c).map(Object::toString).map(String::toUpperCase)
				.collect(Collectors.groupingBy(Function.identity(), LinkedHashMap::new, Collectors.counting()))
				.entrySet().stream().filter(n -> n.getValue() > 1)
				.collect(Collectors.toMap(Map.Entry::getKey, Map.Entry::getValue, (a, b) -> a, LinkedHashMap::new));
				
						// First Reapting Characters & value
//		Entry<String, Long> hashmap = s.chars().mapToObj(c -> (char) c).map(Object::toString).map(String::toUpperCase)
//				.collect(Collectors.groupingBy(Function.identity(), LinkedHashMap::new, Collectors.counting()))
//				.entrySet().stream().filter(n -> n.getValue() == 1).findFirst().get();
//
//		System.out.println(hashmap);
//
//		// Note* --> For an Single Key & value result we need to declare "Entry" to
//		// return
//		// the single key & value pair result.
				
				
				
				//		// find the First Non-repeating Character alone
//		String hashmap = s.chars().mapToObj(c -> (char) c).map(Object::toString).map(String::toUpperCase)
//				.collect(Collectors.groupingBy(Function.identity(), LinkedHashMap::new, Collectors.counting()))
//				.entrySet().stream().filter(n -> n.getValue() == 1).map(v -> v.getKey()).findFirst().get();
				

		System.out.println(hashmap);

	}

}




// Normal way
//
//
//import java.util.*;
//import java.util.stream.*;
//import java.util.Stack;
//import java.util.Queue;
//import java.util.LinkedList;
//
//public class Main {
//
//	public static void main(String[] args) {
//
//		Main obj = new Main();
//
//		Map<Character, Integer> hashmap = new LinkedHashMap<Character, Integer>(); // LinkedHashMap --> Maintains as
//																					// like the given Order
//
//		String s = "Hello world";
//
//		s = s.replaceAll("\\s+", "").toUpperCase();
//
//		for (int i = 0; i < s.length(); i++) {
//
//			char a = s.charAt(i);
//
//			if (!hashmap.containsKey(a)) {
//
//				hashmap.put(a, 0);
//
//			}
//
//			hashmap.put(a, hashmap.get(a) + 1);
//
//		}
//
//		for (Map.Entry<Character, Integer> hash : hashmap.entrySet()) {
//
//			if (hash.getValue() == 1) {
//
//				System.out.println(hash.getKey() + "=>" + hash.getValue());
//				break;
//
//			}
//
//		}
//
//	}
//
//}
//
//
//
